---
description: Continue the last agy-ox conversation with official --conversation or --continue.
argument-hint: "follow-up"
skills: agy-ox
---

Use the `agy-ox` skill as `/agy-ox-continue`. Resume the last `conversation_id` from this chat, or `--continue` if none is known.

$ARGUMENTS
