---
description: Short read-only question for agy-ox. Uses --mode plan, --effort low.
argument-hint: "question"
skills: agy-ox
---

Use the `agy-ox` skill as `/agy-ox-ask`. Force `--mode plan`, `--effort low`, `--print-timeout 2m`.

$ARGUMENTS
