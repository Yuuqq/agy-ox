---
description: Check that the local agy CLI is installed, on PATH, and logged in.
argument-hint: "[optional note]"
skills: agy-ox
---

Use the `agy-ox` skill as `/agy-ox-doctor`. Do not send a `-p` prompt. Check `agy --version` and `agy models`, then report binary, version, and login.

$ARGUMENTS
