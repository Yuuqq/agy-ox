---
description: Implement or fix with agy-ox using --mode accept-edits.
argument-hint: "what to implement or fix"
skills: agy-ox
---

Use the `agy-ox` skill as `/agy-ox-fix`. Force `--mode accept-edits`. Then inspect `git status` and `git diff`.

$ARGUMENTS
